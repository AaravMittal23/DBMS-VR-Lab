{
  "introduction": "Follow these steps to learn the core concepts. You'll see explanations and examples before you test them out in the interactive simulation.",
  "steps": [
    {
      "title": "Step 1: Explore Architecture Components",
      "content": "The DBMS architecture is typically divided into three layers: External (views), Conceptual (logical), and Internal (physical storage). Understanding these layers helps separate user interfaces from physical storage details.",
      "interactiveTask": {
        "prompt": "The Conceptual layer provides Logical Data Independence. It separates the way users view data from the way data is actually stored.",
        "expected": []
      }
    },
    {
      "title": "Step 2: Start the Database Server",
      "content": "Before connecting to the database, you must ensure the background service is running. This is typically done via the terminal.",
      "interactiveTask": {
        "prompt": "We start the database service like a background process:",
        "expected": ["sudo service mysql start"]
      }
    },
    {
      "title": "Step 3: Connect to the Server",
      "content": "Once the server is running, you can connect to it using the mysql client. You need to specify the user (e.g., root) and prompt for a password.",
      "interactiveTask": {
        "prompt": "To log into the MySQL console as the 'root' administrator and prompt for a password, you would use this command:",
        "expected": ["mysql -u root -p"]
      }
    },
    {
      "title": "Step 4: Create a Sandbox Database",
      "content": "A database server can host multiple databases. Let's create a sandbox database for our experiments.",
      "interactiveTask": {
        "prompt": "SQL statements tell the database engine what to do. Always end your SQL commands with a semicolon (;):",
        "expected": ["CREATE DATABASE lab_sandbox;"]
      }
    }
  ],
  "expectedOutcomes": [
    "Understood the three-schema architecture.",
    "Learned the commands to start and connect to the MySQL service.",
    "Learned the SQL syntax for creating a new database."
  ]
}