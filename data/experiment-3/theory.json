{
  "introduction": "Data Manipulation Language (DML) statements are used for managing data within schema objects. Unlike DDL, DML operations manipulate the data itself, not the structure.",
  "sections": [
    {
      "title": "INSERT Command",
      "content": "Used to insert new rows of data into a table.",
      "code": "INSERT INTO Students (ID, Name) VALUES (1, 'Alice');"
    },
    {
      "title": "UPDATE Command",
      "content": "Used to modify the existing records in a table. It is crucial to use the WHERE clause, otherwise all rows will be updated.",
      "code": "UPDATE Students SET Name = 'Alice Smith' WHERE ID = 1;"
    },
    {
      "title": "DELETE Command",
      "content": "Used to delete existing records in a table. Like UPDATE, the WHERE clause is essential to avoid deleting all records.",
      "code": "DELETE FROM Students WHERE ID = 1;"
    },
    {
      "title": "SELECT Command",
      "content": "Although sometimes categorized as DQL (Data Query Language), SELECT is often grouped with DML. It retrieves data from one or more tables.",
      "code": "SELECT * FROM Students;"
    }
  ],
  "realWorldApplication": {
    "title": "Interacting with Spotify",
    "scenario": "Every time you interact with an app, you are triggering DML commands. When you create a Spotify account, the app runs an `INSERT` command. When you rename your playlist, it runs an `UPDATE`. When you remove a song from your library, it runs a `DELETE`. DML is the bridge between the user interface and the data."
  }
}